<UserControl x:Class="SL_0020_Thread.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
        </Style>
    </UserControl.Resources>
    
    
    
    
    <StackPanel>
        <Grid x:Name="gridTest" Background="White">

            <Rectangle Fill="Gold" Stroke="Black" StrokeThickness="3" RadiusX="5" RadiusY="5" />
            <TextBlock x:Name="tbkTimer" Width="300" Height="50" FontSize="30" Foreground="Red" />
        </Grid>
        
        
        
        
        
        
        
        <StackPanel Orientation="Horizontal" >
            <TextBlock  Text="手机号码"/>
            <TextBox x:Name="txtPhone" MaxLength="11" Width="120"/>
            <Button x:Name="btnSend" Content="发送验证码" Click="btnSend_Click" />
            <Border x:Name="border">
                
            </Border>
        </StackPanel>
    </StackPanel>
    
</UserControl>
